# 如何进行深分页

以下是一个简单的深分页场景

```sql
SELECT *
FROM pay_order_ po
         JOIN (SELECT id_
               FROM pay_order_

               ORDER BY id_ LIMIT 10
               OFFSET 6000000) t on po.id_ = t.id_
ORDER BY po.id_;

```
